<UserControl x:Class="WpfApp1.Views.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="700">
    <ScrollViewer>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" />

            <!-- Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Employees by Role Pie Chart -->
                <StackPanel Grid.Column="0" Margin="10">
                    <TextBlock Text="Employees by Role" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <lvc:PieChart x:Name="EmployeesPieChart" LegendLocation="Bottom" Height="300">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend />
                        </lvc:PieChart.ChartLegend>
                    </lvc:PieChart>
                </StackPanel>

                <!-- Clients by Date Pie Chart -->
                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Clients by Date" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <lvc:PieChart x:Name="ClientsPieChart" LegendLocation="Bottom" Height="300">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend />
                        </lvc:PieChart.ChartLegend>
                    </lvc:PieChart>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
