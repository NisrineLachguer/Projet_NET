<UserControl x:Class="WpfApp1.Views.DashboardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             >
    <ScrollViewer>
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Stats Cards Row -->
            <WrapPanel Grid.Row="0" Margin="0,0,0,16">
                <!-- Total Employees Card -->
                <materialDesign:Card Margin="8" Width="200" Height="120">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Total Employees" 
                                 Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                        <StackPanel Grid.Row="1" 
                                  VerticalAlignment="Center">
                            <TextBlock x:Name="TotalEmployees"
                                     Text="0"
                                     Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                     HorizontalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="AccountGroup"
                                                   Width="24"
                                                   Height="24"
                                                   HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </materialDesign:Card>

                <!-- Total Clients Card -->
                <materialDesign:Card Margin="8" Width="200" Height="120">
                    <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Total Clients" 
                                 Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                        <StackPanel Grid.Row="1" 
                                  VerticalAlignment="Center">
                            <TextBlock x:Name="TotalClientsText"
                                     Text="0"
                                     Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                     HorizontalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="AccountMultiple"
                                                   Width="24"
                                                   Height="24"
                                                   HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </materialDesign:Card>
            </WrapPanel>
            
            <materialDesign:Card Margin="16" MaxWidth="900">
                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="400"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title -->
                    <TextBlock Text="Employee Role Distribution"
                               Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,16"/>

                    <!-- Chart Container -->
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Pie Chart -->
                        <Canvas x:Name="ChartCanvas" Grid.Column="0" Margin="16"/>

                        <!-- Legend -->
                        <StackPanel x:Name="LegendPanel" 
                                    Grid.Column="1" 
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center">
                            <TextBlock Text="Legend"
                                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                                       Margin="0,0,0,16"/>
                        </StackPanel>
                    </Grid>

                    <!-- Total Employees -->
                    <TextBlock x:Name="TotalEmployeesText"
                               Grid.Row="2"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,0"/>
                </Grid>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</UserControl>
