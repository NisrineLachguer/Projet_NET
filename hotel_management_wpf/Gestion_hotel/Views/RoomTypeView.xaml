<Window x:Class="WpfApp1.Views.RoomTypeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp1.Views"
        xmlns:viewModels="clr-namespace:WpfApp1.ViewModels"
        mc:Ignorable="d"
        Title="Room Type Management" Height="450" Width="800">
    <Window.DataContext>
        <viewModels:RoomTypeViewModel />
    </Window.DataContext>
    <Grid>
        <DataGrid ItemsSource="{Binding RoomTypes}" AutoGenerateColumns="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" />
                <DataGridTextColumn Header="Capacity" Binding="{Binding Capacity}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Add Room Type" Command="{Binding AddRoomTypeCommand}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" Width="200" />
    </Grid>
</Window>
